</br>
</br>
<h4><%=@organizations.name %></h4>
<table class="table table-hover table-dark">
  <thread>
    <tr>
      <th scope="col">Users</th>
      <th scope="col">Text</th>
      <th scope="col">Status</th>
      <th scope="col"></th>
    </tr>
  </thread>
  <%- @user.each do |user|%>
    <tbody>
    <tr data-item_id="<%= user.name %>">
      <th scope="col"><%= user.name%></th>
      <th scope="col"><%= text_field_tag :text_post, @post.text_post%></th>
      <th scope="col"><%= text_field_tag :status, @post.status%></th>
      <th scope="col"><%= submit_tag "Save",class:"btn btn-large btn-primary" %></th>
    </tr>
    </tbody>
  <% end %>
</table>

